[pytest]
addopts = --strict-markers --tb=native -vv
markers =
    base: Fastest tests, finishing in seconds
    medium: Medium tests, finishing roughly less than a minute
    long: Slowest tests, can take hours