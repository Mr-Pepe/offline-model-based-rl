[pytest]
addopts = --strict-markers --tb=native -v --durations 0 --durations-min=0.1
markers =
    current: Test currently being worked on
    fast: Fastest tests, a few hundred milliseconds
    medium: Medium tests, finishing roughly less than a minute
    slow: Slowest tests, can take hours
testpaths =
    benchmark/test